/**
 * Согласие кандидата на обработку персональных данных (BR-33).
 *
 * ⚠️ ТЕКСТ — ЗАГЛУШКА, НЕ ЮРИДИЧЕСКИЙ ДОКУМЕНТ.
 *
 * Перед работой с реальными кандидатами заменить на текст, выверенный
 * юристом, и одновременно сменить версию: она хранится вместе с фактом
 * согласия, и по ней при разборе обращения восстанавливают, на что
 * именно человек соглашался. Открытый вопрос 3 раздела 19 ТЗ.
 *
 * Пока версия равна метке черновика, приложение не поднимется в проде
 * (см. lib/config/production-check). Заглушку легко не заметить именно
 * потому, что она работает: форма открывается, галочка ставится,
 * согласие пишется в базу — и отличить его от настоящего можно будет
 * только постфактум, по этой строке внутри записи.
 *
 * Лежит в lib/legal, а не рядом с сервисом, по двум причинам. Это
 * юридический текст, а не работа с базой, и соседям по папке —
 * реквизитам оператора и текстам согласий на сайте — он родня. И это
 * модуль без зависимостей: проверку при старте видит edge-рантайм,
 * а туда ни Prisma, ни node:crypto не тянутся.
 *
 * Имя версии отличается от CONSENT_VERSION в consent-texts намеренно:
 * там версия текстов на сайте (галочки под формой заявки), здесь —
 * версия согласия кандидата. Два разных документа, и одинаковое имя
 * у них однажды уже сбило с толку.
 */

/** Пока версия равна этому, текст юристом не выверен. */
export const DRAFT_CANDIDATE_CONSENT_VERSION = "draft-1";

/** Версия текста, под которым кандидат поставил галочку. */
export const CANDIDATE_CONSENT_VERSION: string =
  DRAFT_CANDIDATE_CONSENT_VERSION;

export const CANDIDATE_CONSENT_TEXT = `Я даю согласие на обработку моих персональных данных, указанных
в резюме и переданных в ходе общения, в целях рассмотрения моей
кандидатуры на вакансии и передачи их потенциальным работодателям.

Обработка включает сбор, хранение, уточнение, использование и передачу
работодателю, в интересах которого ведётся подбор.

Согласие действует один год и может быть отозвано в любой момент
по письменному обращению.`;
