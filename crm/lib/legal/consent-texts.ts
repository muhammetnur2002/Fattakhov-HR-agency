/**
 * Тексты согласий на сайте.
 *
 * Дословно из юридического пакета (раздел 10). Менять их можно
 * только вместе с пакетом и только через новую версию: закон
 * требует, чтобы оператор мог доказать, под чем именно человек
 * поставил галочку, а «под текстом, который тогда был на сайте»
 * доказательством не является.
 *
 * Поэтому версия хранится вместе с заявкой. Правите текст -
 * поднимаете версию, старые заявки остаются привязаны к старой.
 */

/** Адрес для обращений по персональным данным. */
export const PRIVACY_EMAIL = "privacy@fattakhovhr.ru";

/** Публичный адрес политики обработки. */
export const PRIVACY_POLICY_PATH = "/privacy";

/** Оператор. Реквизиты из юридического пакета. */
export const OPERATOR_NAME =
  "Индивидуальный предприниматель Фаттахов Тимур Айратович";

/**
 * Реквизиты оператора. Раньше лежали строками прямо в политике,
 * а нужны ещё и в подвале сайта: ИНН и адрес на видном месте —
 * привычный признак того, что за сайтом стоит настоящее лицо,
 * а не лендинг-однодневка. Держим рядом с именем оператора,
 * чтобы правились вместе с ним.
 */
export const OPERATOR_INN = "160402219594";
export const OPERATOR_OGRNIP = "321169000137246";
export const OPERATOR_ADDRESS = "г. Казань, ул. Агрономическая, д. 18";

/**
 * Версия текстов.
 *
 * Меняется при любой правке формулировок ниже. Дата в версии, а не
 * порядковый номер: при разборе обращения субъекта первым делом
 * нужно понять, что было написано на сайте в тот день.
 */
export const CONSENT_VERSION = "2026-08-13";

/**
 * Обязательное согласие для отправки заявки.
 *
 * Без него форма не отправляется - это требование раздела 10
 * пакета и статьи 9 закона в редакции с 01.09.2025: согласие
 * оформляется отдельно от иных документов.
 */
export const LEAD_CONSENT_TEXT =
  `Я даю ${OPERATOR_NAME} согласие на обработку моего имени, компании, ` +
  "телефона, email и содержания заявки в целях ответа на обращение " +
  "и ведения переговоров. Согласие действует 90 дней после последнего " +
  "содержательного контакта, если ранее не возникнет иное законное " +
  `основание. Я могу отозвать согласие по адресу ${PRIVACY_EMAIL}.`;

/**
 * Необязательное согласие на рекламу.
 *
 * Отдельное и добровольное: статья 18 закона о рекламе требует
 * предварительного согласия именно на рекламные сообщения, и
 * «заодно с заявкой» его получить нельзя. Обязательным быть
 * не должно никогда.
 */
export const MARKETING_CONSENT_TEXT =
  "Я согласен(на) получать от Fattakhov HR информационные и рекламные " +
  "сообщения по указанным мной каналам связи. Согласие добровольное " +
  "и не является условием отправки заявки. Я могу отказаться от сообщений " +
  `в любой момент способом, указанным в сообщении, либо по адресу ${PRIVACY_EMAIL}.`;
