-- Номер счёта не имеет права повторяться.
--
-- Раньше он считался запросом «сколько счетов уже есть» и подставлялся
-- отдельной вставкой. Два счёта, выписанных одновременно, читали одно
-- и то же число и получали один номер: на восьми одновременных вставках
-- различных номеров выходило три. Проверка в коде такую гонку не ловит
-- по определению — остановить её может только база.
-- CreateIndex
CREATE UNIQUE INDEX "Invoice_organizationId_number_key" ON "Invoice"("organizationId", "number");
